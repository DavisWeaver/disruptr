#' Function to generate a null distribution of change in network potential for a given set of genes
#'
#' This is accomplished by randomly permuting the nodes in a PPI n times and re-calculating the change in network potential for each permutation
#'
#' @inheritParams node_repression
#'
#' @export
#'

null_np <- function(g, exp, v_rm) {

}
